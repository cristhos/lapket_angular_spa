import { Routes } from '@angular/router';
import { NotificationListComponent } from '../notification/notification.component';

export const NOTIFICATION_ROUTING : Routes = [
      {path: '', component: NotificationListComponent},
];
